display_name: Feature Implement
short_description: Implement validated feature plans through guarded merge-unit branches and PRs.
default_prompt: Use feature:implement to execute a validated feature plan one merge unit at a time with guarded branch, PR, review, merge, and resume behavior.
