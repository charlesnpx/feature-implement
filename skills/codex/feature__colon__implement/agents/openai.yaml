interface:
  display_name: "Feature Implement"
  short_description: "Run guarded feature plans"
  default_prompt: "Use $feature:implement to execute a validated feature plan one merge unit at a time."

policy:
  allow_implicit_invocation: false
