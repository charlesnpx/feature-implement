display_name: Feature
short_description: Create validated epic, feature, story, and merge-unit plans for implementation.
default_prompt: Use feature to break implementation scope into epics, features, stories, and merge units, materialize the plan folder, review it, and validate the resulting plan lock.
