interface:
  display_name: "Feature"
  short_description: "Create validated feature plans"
  default_prompt: "Use $feature to break implementation scope into epics, features, stories, and merge units, then validate the plan."

policy:
  allow_implicit_invocation: false
